## Custom NDK Sysroot path

xbuild downloads automatically the android ndk from the releases github page. If you want to use a custom ndk version specify the following two environment variables:
`ANDROID_NDK_ROOT` and `ANDROID_NDK_SYSROOT` and `ANDROID_HOME`
